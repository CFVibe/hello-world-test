# 2026-02-19

## Morning briefing feedback (Christian)
- News must be strictly actionable and directly relevant to Christian’s goals/profile; avoid broad/general industry items (example: India manufacturing partnerships not relevant).
- Always deliver the full briefing structure even if parts fail (use fallbacks, still provide what exists).
- Include NotebookLM links in Slack (not just email). If link not available, explicitly state why and what to do.
- Include in future briefings:
  - Ideas: 1 and 3 (Today’s focus + Tool workflow)
  - Improvements: 1,2,3,5
  - Product idea: 1
- NotebookLM artifact generation likely hit free-generation limits earlier; Christian upgraded to Google Pro plan (NotebookLM/Gemini) so quotas should be higher now.
- Profile tweak: remove LMS specifically; keep “vibe coding” generally.

## Preference: subagents for coding
- Christian request: always use a subagent for coding tasks using Codex 5.3 (or the coding agent skill), instead of coding directly in the main session.

## Calendar preference
- Use Google Calendar (Christian can see Jarvis calendar; Jarvis can see Christian calendar; they are synced).

## App lifecycle workflow preference
- Desired workflow: Christian asks “create app X that does Y” -> Jarvis creates folder/repo named X, deploys, and makes it available at X.christianfransson.com.
- Default exposure: PRIVATE (behind Cloudflare Access). If Christian says “public”, make it publicly accessible.
- Deletion workflow: “delete app X” -> Jarvis deletes the app locally and removes DNS/tunnel routing; also delete the GitHub repo one day later via scheduled cron.
